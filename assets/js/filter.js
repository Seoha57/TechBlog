document.addEventListener('DOMContentLoaded',()=>{const links=document.querySelectorAll('[data-filter]');const posts=document.querySelectorAll('[data-categories]');links.forEach(link=>link.addEventListener('click',event=>{event.preventDefault();const filter=link.dataset.filter;links.forEach(item=>item.classList.toggle('active',item===link));posts.forEach(post=>{post.hidden=filter!=='all'&&!post.dataset.categories.split(',').includes(filter)})}))});
